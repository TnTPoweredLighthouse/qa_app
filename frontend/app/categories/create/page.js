import css from "../../main-view-placeholder.module.css"

export default async function CreateCategory() {

    return (
        <div className={css.container}>Create new category [in progress]</div>
    );
}
